---
marp: true
theme: default
paginate: true
backgroundColor: #e6eefc
style: |
  section {
    background-color: #ffffff;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    padding: 1px 40px;
  }
  h1, h2, h3 {
    text-align: center;
    margin-top: 0;
    padding-top: 10px;
  }
  h1 {
    color: #2c3e50;
    border-bottom: 3px solid #3498db;
    padding-bottom: 15px;
    margin-bottom: 30px;
  }
  h2 {
    color: #34495e;
    margin-bottom: 25px;
  }
  h3 {
    color: #16a085;
    margin-bottom: 20px;
  }
  code {
    background-color: #f8f9fa;
    border-radius: 4px;
    padding: 2px 6px;
    font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
  }
  pre {
    background-color: #f8f9fa;
    border-left: 4px solid #3498db;
    padding: 15px;
    border-radius: 0 4px 4px 0;
    font-size: 0.9em;
    overflow-x: auto;
  }
  table {
    border-collapse: collapse;
    width: 100%;
    margin: 20px 0;
  }
  th {
    background-color: #3498db;
    color: white;
    padding: 10px;
    text-align: left;
  }
  td {
    padding: 10px;
    border-bottom: 1px solid #ddd;
  }
  tr:nth-child(even) {
    background-color: #f8f9fa;
  }
  ul, ol {
    margin: 15px 0;
    padding-left: 25px;
  }
  li {
    margin: 5px 0;
  }
  .columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 30px;
    align-items: start;
  }
  .columns-3 {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 20px;
  }
  .warning {
    background-color: #fff3cd;
    border-left: 4px solid #ffc107;
    padding: 15px;
    margin: 15px 0;
    border-radius: 0 4px 4px 0;
  }
  .info {
    background-color: #d1ecf1;
    border-left: 4px solid #17a2b8;
    padding: 15px;
    margin: 15px 0;
    border-radius: 0 4px 4px 0;
  }
  .success {
    background-color: #d4edda;
    border-left: 4px solid #28a745;
    padding: 15px;
    margin: 15px 0;
    border-radius: 0 4px 4px 0;
  }
  .lead {
    text-align: center;
    font-size: 1.2em;
    color: #2c3e50;
  }
---

<!-- _class: lead -->
# **Лекция 2: C#: Значимые и ссылочные типы. <br>  Классы и методы.  Массивы и строки**

**🎯 Цель лекции:** 
- 🧠 Научить студентов понимать различия в хранении данных
- ⚙️ Организовывать код с помощью классов и методов 
- 📝 Работать с коллекциями данных и текстом

---

<!-- _class: lead -->
# **📋 Слайд 2: План лекции**

<div class="columns-3">
<div>

### 🎯 Раздел 1
Значимые и ссылочные типы данных

### 🏗️ Раздел 2
Классы и объекты

</div>
<div>

### ⚙️ Раздел 3
Методы и их типы

### 📊 Раздел 4
Массивы как коллекции элементов

</div>
<div>

### 📝 Раздел 5
Работа со строками и StringBuilder

### 💻 Раздел 6
Практические задачи

</div>
</div>

---

## **Слайд 3: Значимые типы (Value Types)**

**Определение:** Типы данных, хранящие значения непосредственно в стеке

**💡 Характеристики:**
- Хранятся в стеке (stack)
- Переменная содержит само значение
- Быстрый доступ к памяти
- Автоматическое освобождение памяти

**🔢 Примеры:**
- `int`, `double`, `bool`
- `char`, `struct`, `enum`

---

## **Слайд 4: Ссылочные типы (Reference Types)**

**Определение:** Типы данных, хранящие ссылку на объект в управляемой куче

**💡 Характеристики:**
- Хранятся в куче (heap)
- Переменная содержит ссылку/адрес
- Управляются сборщиком мусора
- Размер может меняться во время выполнения

**📦 Примеры:**
- `string`, `Array`
- `class`

---

## **Слайд 5: Сравнительная таблица**

| Критерий | Значимые типы | Ссылочные типы |
|----------|---------------|----------------|
| **📂 Хранение** | Стек | Куча |
| **📝 Содержимое** | Значение | Ссылка |
| **🔄 Присваивание** | Копия значения | Копия ссылки |
| **⚫ Значение по умолчанию** | 0, false | null |

---

## **Слайд 6: Практический пример различий**

```csharp
1  // Значимый тип
2  int a = 5;
3  int b = a;  // Копия значения
4  b = 10;     // a=5, b=10
5  
6  // Ссылочный тип
7  int[] arr1 = {1, 2, 3};
8  int[] arr2 = arr1;    // Копия ссылки
9  arr2[0] = 99;         // arr1[0] тоже = 99!
```

---

## **Слайд 7: Ошибки при работе с типами**

<div class="warning">

**❌ Присваивание null значимому типу:**
```csharp
1  int age = null; // Ошибка!
2  int? nullableAge = null; // Правильно
```

</div>

<div class="info">

**⚠️ Непонимание разницы при присваивании:**
```csharp
1  // Значимые: копируется значение
2  int a = 5, b = a; // Независимые
3  b = 10; // a=5, b=10
4  
5  // Ссылочные: копируется ссылка
6  int[] arr1 = {1}, arr2 = arr1; // Общие данные
7  arr2[0] = 99; // arr1[0] тоже = 99
```

</div>

---

## **Слайд 8: Null и Nullable типы**

**Null:**
- 🔗 Специальное значение для ссылочных типов
- 🎯 Указывает на отсутствие объекта
- ❗ `NullReferenceException` при обращении к null

**Nullable типы:**
```csharp
1  int? nullableAge = null;
2  if (nullableAge.HasValue)
3  {
4      int age = nullableAge.Value;
5  }
6  // Или:
7  int safeAge = nullableAge ?? 0;
```

---

## **Слайд 9: Ошибки с null**

<div class="warning">

**🚫 Работа с null-ссылкой:**
```csharp
1  Person p = null;
2  p.Name = "Текст"; // NullReferenceException
```

</div>

<div class="info">

**✅ Правильная проверка на null:**
```csharp
1  void ProcessPerson(Person p)
2  {
3      // Опасно:
4      Console.WriteLine(p.Name);
5      // Лучше:
6      if (p != null) Console.WriteLine(p.Name);
7  }
```

</div>

---

## **Слайд 10: Классы**

**📝 Определение:** Пользовательский тип данных, описывающий структуру и поведение объектов

**📦 Состав класса:**
- 📊 Поля (данные)
- ⚙️ Методы (поведение)
- 🔧 Конструкторы (инициализация)
- 🔐 Свойства (доступ к данным)

```csharp
1  public class Person
2  {
3      public string Name;
4      public int Age;
5  }
```

---

## **Слайд 11: Объекты**

**📝 Определение:** Конкретный экземпляр класса, созданный в памяти

**✨ Создание объекта:**
```csharp
1  Person person1 = new Person();
2  person1.Name = "Анна";
3  person1.Age = 25;
```

**🔗 Ссылочная природа:**
```csharp
1  Person person2 = person1;  // Копия ссылки
2  person2.Age = 26;          // person1.Age тоже = 26
```

---

## **Слайд 12: Ошибки при работе с классами**

<div class="warning">

**⚠️ Непонимание ссылочной природы классов:**
```csharp
1  Person p1 = new Person { Name = "Анна" };
2  Person p2 = p1; // Копия ссылки!
3  p2.Name = "Иван"; // p1.Name тоже "Иван"
```

</div>

<div class="info">

**❌ Вызов нестатического метода без объекта:**
```csharp
1  Person.Introduce(); // Ошибка!
2  // Правильно:
3  Person p = new Person();
4  p.Introduce();
```

</div>

---

## **Слайд 13: Конструкторы**

**📝 Определение:** Специальный метод для инициализации объекта

```csharp
1  public class Student
2  {
3      public string Name;
4      public int Course;
5      
6      // Конструктор
7      public Student(string name, int course)
8      {
9          Name = name;
10         Course = course;
11     }
12 }
```

**🎯 Использование:**
```csharp
1  Student student = new Student("Мария", 2);
```

---

## **Слайд 14: Модификаторы доступа**

**🔧 Виды модификаторов:**
- 🌐 `public` — доступен из любого места
- 🔒 `private` — доступен только внутри класса
- 🛡️ `protected` — доступен в классе и наследниках
- 📦 `internal` — доступен в пределах сборки

```csharp
1  public class BankAccount
2  {
3      private decimal balance;  // private
4      public string OwnerName;  // public
5  }
```

---

## **Слайд 15: Методы**

**📝 Определение:** Блок кода, выполняющий задачу и вызываемый по имени

**📝 Структура:**
```csharp
1  public int Add(int a, int b)
2  {
3      return a + b;
4  }
```

**🎫 Сигнатура метода:**
- 📛 Имя + типы параметров
- 🔙 Возвращаемый тип НЕ входит в сигнатуру
- 📋 Порядок параметров важен

---

## **Слайд 16: Типы методов**

**🔙 Методы с возвращаемым значением:**
```csharp
1  public int Multiply(int x, int y)
2  {
3      return x * y;
4  }
```

**⚫ Void-методы:**
```csharp
1  public void PrintMessage(string text)
2  {
3      Console.WriteLine(text);
4  }
```

**🔄 Return в void-методах:**
- 🔙 Не возвращает значение
- 🚪 Используется для досрочного выхода

---

## **Слайд 17: Ошибки в методах**

<div class="warning">

**❌ Использование void-метода как значения:**
```csharp
1  int x = PrintMessage("текст"); // Ошибка!
```

</div>

<div class="info">

**⚠️ Отсутствие return в методе с возвращаемым типом:**
```csharp
1  int GetMax(int a, int b)
2  {
3      if (a > b) return a; // А если a <= b?
4  }
```

</div>

<div class="warning">

**🚫 Несоответствие типа возвращаемого значения:**
```csharp
1  int GetNumber() { return "строка"; } // Ошибка!
```

</div>

---

## **Слайд 18: Статические методы**

**📝 Определение:** Принадлежат классу, а не объекту

**✨ Особенности:**
- 📛 Вызываются через имя класса
- 🚫 Не могут работать с нестатическими полями
- 🛠️ Используются для утилитарных функций

```csharp
1  public static double CircleArea(double r)
2  {
3      return Math.PI * r * r;
4  }
5  // Вызов:
6  double area = MathHelper.CircleArea(5);
```

---

## **Слайд 19: Нестатические методы**

**📝 Определение:** Принадлежат конкретному объекту

**✨ Особенности:**
- 🎯 Вызываются через объект
- 💼 Работают с данными объекта
- 📊 Используют поля объекта

```csharp
1  public class Calculator
2  {
3      private int baseValue;
4      public int MultiplyBy(int x)
5      {
6          return baseValue * x;
7      }
8  }
```

---

## **Слайд 20: Параметры по значению**

**📝 Стандартный способ:**
- 📋 Передается копия значения
- 🔒 Изменения внутри метода не видны снаружи
- ⚙️ Используется по умолчанию

```csharp
1  void TrySwap(int a, int b)
2  {
3      int temp = a;
4      a = b;
5      b = temp;
6      // a и b не изменились снаружи
7  }
```

---

## **Слайд 21: Параметры по ссылке (ref)**

**📝 Модификатор ref:**
- 🔗 Передается ссылка на переменную
- 👁️ Изменения видны снаружи
- ✅ Переменная должна быть инициализирована

```csharp
1  void RealSwap(ref int a, ref int b)
2  {
3      int temp = a;
4      a = b;
5      b = temp;
6  }
7  // Использование:
8  RealSwap(ref x, ref y);
```

---

## **Слайд 22: Параметры по ссылке (out)**

**📝 Модификатор out:**
- 🔗 Как ref, но метод обязан присвоить значение
- ⚡ Переменная не требует инициализации
- 🔄 Используется для возврата нескольких значений

```csharp
1  bool TryParsePositive(string s, out int result)
2  {
3      result = 0; // Обязательно!
4      if (int.TryParse(s, out int num) && num > 0)
5      {
6          result = num;
7          return true;
8      }
9      return false;
10 }
```

---

## **Слайд 23: Параметры по ссылке (in)**

**📝 Модификатор in:**
- 🔗 Передача по ссылке только для чтения
- 🛡️ Защищает от изменений
- ⚡ Повышает производительность для структур

```csharp
1  double CalculateDistance(in Point p1, in Point p2)
2  {
3      // p1.X = 10; // Ошибка компиляции!
4      return Math.Sqrt(Math.Pow(p2.X - p1.X, 2) + 
5                       Math.Pow(p2.Y - p1.Y, 2));
6  }
```

---

## **Слайд 24: Массивы**

**📝 Определение:** Структура данных для хранения фиксированного количества элементов одного типа

**✨ Создание массива:**
```csharp
1  int[] numbers = new int[5];      // Все 0
2  int[] values = {1, 2, 3, 4, 5};  // С инициализацией
```

**🎯 Доступ к элементам:**
- 🔢 Индексация с 0
- `numbers[0] = 10;`
- `int x = numbers[1];`

---

## **Слайд 25: Массивы как ссылочный тип**

```csharp
1  int[] arr1 = {1, 2, 3};
2  int[] arr2 = arr1;  // Копия ссылки
3  arr2[0] = 99;       // arr1[0] тоже = 99
4  
5  // Метод изменяет массив
6  void ModifyArray(int[] array)
7  {
8      array[0] = 100; // Изменения видны снаружи
9  }
10 ModifyArray(arr1);
```

---

## **Слайд 26: Ошибки при работе с массивами**

<div class="warning">

**🚫 Выход за границы массива:**
```csharp
1  int[] arr = {1, 2};
2  int x = arr[2]; // IndexOutOfRangeException
```

</div>

<div class="info">

**⚠️ Путаница Length/GetLength для многомерных:**
```csharp
1  int[,] matrix = new int[3, 4];
2  int total = matrix.Length; // 12
3  int rows = matrix.GetLength(0); // 3
4  int cols = matrix.GetLength(1); // 4
```
</div>

<div class="warning">

**❌ Попытка изменить размер массива:**
```csharp
1  int[] arr = new int[5];
2  // arr.Length = 10; // Нельзя!
```

</div>

---

## **Слайд 27: Методы для работы с массивами**

```csharp
1  // Заполнение случайными числами 🎲
2  void FillRandom(int[] array, int min, int max)
3  {
4      Random rnd = new Random();
5      for (int i = 0; i < array.Length; i++)
6          array[i] = rnd.Next(min, max);
7  }
8  
9  // Поиск максимального элемента 🔝
10 int FindMax(int[] array)
11 {
12     if (array.Length == 0) return 0;
13     int max = array[0];
14     for (int i = 1; i < array.Length; i++)
15         if (array[i] > max) max = array[i];
16     return max;
17 }
```

---

## **Слайд 28: Встроенные методы Array**

```csharp
1  int[] numbers = {5, 2, 8, 1, 9};
2  
3  Array.Sort(numbers);      // {1, 2, 5, 8, 9}
4  Array.Reverse(numbers);   // {9, 8, 5, 2, 1}
5  
6  int index = Array.IndexOf(numbers, 5); // 2
7  
8  bool exists = Array.Exists(numbers, x => x > 10);
9  
10 Array.Resize(ref numbers, 10);
```

---

## **Слайд 29: Строки (string)**

**📝 Определение:** Неизменяемый (immutable) ссылочный тип, последовательность символов

**🔄 Неизменяемость:**
```csharp
1  string text = "Hello";
2  text = text + " World"; // Создается НОВАЯ строка
3  // "Hello" остается в памяти до сборки мусора
```

**🐌 Проблема производительности:**
```csharp
1  string result = "";
2  for (int i = 0; i < 10000; i++)
3      result += i; // 10000 новых объектов!
```

---

## **Слайд 30: Ошибки при работе со строками**

<div class="warning">

**🐌 Конкатенация в цикле через `+`:**
```csharp
1  string result = "";
2  for (int i = 0; i < 10000; i++)
3      result += i; // Медленно!
4  // Используйте StringBuilder
```

</div>

<div class="info">

**❌ Забыть ToString() у StringBuilder:**
```csharp
1  StringBuilder sb = new StringBuilder();
2  sb.Append("Text");
3  string result = sb; // Ошибка!
4  string result = sb.ToString(); // Правильно
```

</div>

---

## **Слайд 31: Операции со строками**

```csharp
1  string text = " Hello, World! ";
2  
3  // Основные методы
4  int length = text.Length;          // 15
5  string trimmed = text.Trim();      // "Hello, World!"
6  string upper = text.ToUpper();     // " HELLO, WORLD! "
7  int index = text.IndexOf("World"); // 8
8  
9  // Разделение и объединение
10 string[] words = text.Split(',');
11 string joined = string.Join("-", words);
```

---

## **Слайд 32: StringBuilder**

**📝 Определение:** Класс для эффективной работы с изменяемыми строками

**🎯 Для чего нужен:**
- 🔄 Частые операции конкатенации
- 🔁 Изменение строк в циклах
- 🚫 Избегание создания множества объектов

```csharp
1  using System.Text;
2  StringBuilder sb = new StringBuilder();
```

---

## **Слайд 33: Методы StringBuilder**

```csharp
1  StringBuilder sb = new StringBuilder("Start");
2  
3  sb.Append(" and continuation");
4  sb.AppendLine(); // Перевод строки
5  sb.AppendFormat("Value: {0}", 42);
6  sb.Insert(5, "inserted ");
7  sb.Remove(0, 3);
8  sb.Replace("old", "new");
9  
10 string result = sb.ToString();
11 sb.Clear();
```

<div class="success">

**🚀 Производительность:** Конкатенация 10000 строк:
- `string`: ~100 мс 🐌
- `StringBuilder`: < 1 мс ⚡

</div>

---

<!-- _class: lead -->
# **Слайд 34: Задачи для практики**

<div class="columns">
<div>

## **📋 Блок 1: Передача параметров**
1️⃣ Swap с использованием `ref`
2️⃣ Безопасное вычисление среднего с `out`
3️⃣ Вычисление расстояния с `in`

## **📊 Блок 2: Методы и массивы**
1️⃣ Фильтрация положительных чисел
2️⃣ Поиск всех индексов строки
3️⃣ Циклический сдвиг массива

</div>
<div>

## **📝 Блок 3: Строки и методы**
1️⃣ Анализ строки (буквы, цифры, пробелы)
2️⃣ Форматирование номера телефона
3️⃣ Обработка CSV строки

## **🏗️ Блок 4: Классы и методы**
1️⃣ Класс "Прямоугольник"
2️⃣ Класс "Счетчик"
3️⃣ Класс "Учет студентов"

</div>
</div>

---

<!-- _class: lead -->
# **Слайд 35: Следующая лекция**

## **Лекция 3: Анализ сложности алгоритмов. Списки**

**🎯 Цель:** Заложить основу для оценки алгоритмов и изучить динамические структуры данных

**📋 Содержание:**
1️⃣ Анализ алгоритмов (время, память)
2️⃣ Асимптотическая сложность (O-нотация)
3️⃣ Списки `List<T>`
4️⃣ Практическое сравнение структур данных

---

<!-- _class: lead -->
# **Слайд 36: Спасибо за внимание!**

## **Вопросы❓**
