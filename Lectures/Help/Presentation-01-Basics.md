---
marp: true
theme: default
paginate: true
backgroundColor: #e6eefc
style: |
  section {
    background-color: #ffffff;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    padding: 1px 40px;
  }
  h1, h2, h3 {
    text-align: center;
    margin-top: 0;
    padding-top: 10px;
  }
  h1 {
    color: #2c3e50;
    border-bottom: 3px solid #3498db;
    padding-bottom: 15px;
    margin-bottom: 30px;
  }
  h2 {
    color: #34495e;
    margin-bottom: 25px;
  }
  h3 {
    color: #16a085;
    margin-bottom: 20px;
  }
  code {
    background-color: #f8f9fa;
    border-radius: 4px;
    padding: 2px 6px;
    font-family: 'Consolas', 'Monaco', 'Courier New', monospace;
  }
  pre {
    background-color: #f8f9fa;
    border-left: 4px solid #3498db;
    padding: 15px;
    border-radius: 0 4px 4px 0;
    font-size: 0.9em;
    overflow-x: auto;
  }
  table {
    border-collapse: collapse;
    width: 100%;
    margin: 20px 0;
  }
  th {
    background-color: #3498db;
    color: white;
    padding: 10px;
    text-align: left;
  }
  td {
    padding: 10px;
    border-bottom: 1px solid #ddd;
  }
  tr:nth-child(even) {
    background-color: #f8f9fa;
  }
  ul, ol {
    margin: 15px 0;
    padding-left: 25px;
  }
  li {
    margin: 5px 0;
  }
  .columns {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 30px;
    align-items: start;
  }
  .columns-3 {
    display: grid;
    grid-template-columns: 1fr 1fr 1fr;
    gap: 20px;
  }
  .warning {
    background-color: #fff3cd;
    border-left: 4px solid #ffc107;
    padding: 15px;
    margin: 15px 0;
    border-radius: 0 4px 4px 0;
  }
  .info {
    background-color: #d1ecf1;
    border-left: 4px solid #17a2b8;
    padding: 15px;
    margin: 15px 0;
    border-radius: 0 4px 4px 0;
  }
  .success {
    background-color: #d4edda;
    border-left: 4px solid #28a745;
    padding: 15px;
    margin: 15px 0;
    border-radius: 0 4px 4px 0;
  }
  .lead {
    text-align: center;
    font-size: 1.2em;
    color: #2c3e50;
  }
---


# **Презентация к лекции 1**
## **Введение в C#. Базовый синтаксис**

**Направление:** «Информационные системы и технологии»  
**Преподаватель:** Крюкова Яна Сергеевна  
*Кандидат технических наук, доцент кафедры ИСТ*

<!-- _class: lead -->

---
### **Слайд 1: Структура курса (8 лекций)**

| № | Тема | 
|---|------|
| 1 | **Введение в C#**. Базовый синтаксис, типы данных, управляющие конструкции |
| 2 | **C#: Методы, массивы, основы ООП**. Значимые и ссылочные типы. String и StringBuilder |
| 3 | **Анализ сложности алгоритмов**. Простые структуры данных: списки (List<T>) |
| 4 | **Алгоритмы сортировки и поиска**. Quicksort, Mergesort, бинарный поиск |
| 5 | **Стеки, очереди, хеш-таблицы**. Stack<T>, Queue<T>, Dictionary, HashSet |
| 6 | **Деревья**. Бинарные деревья поиска (BST) |
| 7 | **Графы**. Gредставление, обход, алгоритмы (DFS, BFS) |
| 8 | **Жадные алгоритмы и ДП**. Динамическое программирование (введение) |

---
### **Слайд 2: Формат курса и ресурсы**

**Формат курса:**
- 📚 **Лекции:** 1 раз в 2 недели
- 💻 **Практические занятия:** 1 раз в неделю
- 📝 **Форма контроля:** Зачёт

<br>

**Полезные ресурсы:**
- 🔗 **Документация:** [docs.microsoft.com/dotnet/csharp](https://docs.microsoft.com/dotnet/csharp)
- 🎓 **Курсы:** [Microsoft Learn](https://learn.microsoft.com), [metanit.com](https://metanit.com/sharp/)
- 🏆 **Практика:** [LeetCode](https://leetcode.com), [Codewars](https://codewars.com)

---
### **Слайд 3: Цели лекции 1**

**🎯 Тема:** Введение в C#. Базовый синтаксис  

**Цели:**  
- ✅ Познакомиться с языком C# и платформой .NET  
- ✅ Освоить минимальный синтаксис для написания программ  
- ✅ Научиться использовать переменные, операторы и циклы  

<br>

**Практический итог:**  
К концу лекции вы сможете писать простые консольные программы.

---
### **Слайд 4: Почему C# и .NET?**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**💎 C# (Си-шарп):**  
- Современный объектно-ориентированный язык  
- Разработан Microsoft  
- Активно используется в индустрии  

</div>
<div>

**🌐 .NET (дот-нет):**  
- Платформа для выполнения программ на C#  
- Предоставляет среду выполнения и библиотеки  

</div>
</div>

**📱 Области применения C#:**  
1. Backend-сервисы и веб (ASP.NET Core)  
2. Десктопные приложения (Windows)  
3. Мобильная и игровая разработка (Xamarin, Unity)

---
### **Слайд 5: Структура первой программы**

```csharp
1.  using System;  
2.  namespace AlgorithmsCourse
3.  {
4.      class Program
5.      {
6.          static void Main(string[] args)
7.          {
8.              Console.WriteLine("Hello, World!");
9.         }
10.     }
11. }
```

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 15px;">
<div>

| Элемент | Описание |
|---------|----------|
| `1. using System;` | Подключение библиотеки |
| `7. Main(...)` | Главный метод, старт |
| `9. Console.WriteLine()` | Вывод текста |
| `{ }` | Блоки кода |

</div>
</div>

---
### **Слайд 6: Переменные и типы данных**

**💾 Переменная** – ячейка памяти для хранения данных.  
**📊 Тип данных** определяет, что можно хранить.

| Тип C# | Описание | Пример |
|--------|----------|--------|
| `int` | Целое число | `42`, `-7` |
| `double` | Число с дробью | `3.14`, `-2.5` |
| `bool` | Логическое значение | `true`, `false` |
| `string` | Текст (строка) | `"Привет"` |
| `char` | Один символ | `'A'`, `'?'` |

---

### **Слайд 7: Объявление переменных**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**📝 Объявление и инициализация:**
```csharp
1.  int age = 25;
2.  double price = 99.95;
3.  bool isActive = true;
4.  string name = "Анна";
5.  char grade = 'A';
```

</div>
<div>

**🔤 Использование `var`:**
```csharp
1.  var count = 10;     // int
2.  var message = "Ошибка!"; // string
3.  var average = 4.5;  // double
```

**📌 Особенности `var`:**
- Компилятор определяет тип автоматически (удобно для сокращения записи)
- Тип фиксируется при первой инициализации

</div>
</div>

---

### **Слайд 8: Приведение типов**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**🔄 Неявное приведение** – автоматическое:
```csharp
1.  int small = 100;
2.  double big = small; // int → double
3.  // big = 100.0
```

**❗ Явное приведение** – с указанием типа:
```csharp
1.  double price = 9.95;
2.  int rounded = (int)price; // double → int
3.  // rounded = 9
```

</div>
<div>

| Тип | Описание | Пример |
|-----|----------|--------|
| **Неявное** | Автоматическое, нет потери данных | `int → double` |
| **Явное** | Требует указания типа, возможна потеря | `double → int` |

</div>
</div>

---
### **Слайд 9: Класс `Convert`**

```csharp
1.  string text = "123";
2.  int number = Convert.ToInt32(text);
3.  // text → int, number = 123
4.  
5.  double value = 45.67;
6.  int intVal = Convert.ToInt32(value);
7.  // double → int (округление)
8.  
9.  bool flag = true;
10. int boolAsInt = Convert.ToInt32(flag);
11. // true → 1, false → 0
```

**📋 Основные методы:**
- `Convert.ToInt32(string)` – строка в int
- `Convert.ToDouble(string)` – строка в double
- `Convert.ToString(int)` – int в строку
- `Convert.ToBoolean(string)` – строка в bool

---
### **Слайд 10: `Convert` vs `Parse()`**

```csharp
1.  string input = null;
2.  
3.  // Convert - безопаснее
4.  int result1 = Convert.ToInt32(input);
5.  // result1 = 0
6.  
7.  // Parse - выбрасывает исключение
8.  int result2 = int.Parse(input);
9.  // ОШИБКА! System.ArgumentNullException
```

**🔍 Ключевое отличие:**
`Convert` не выбрасывает исключение для `null`, а возвращает значение по умолчанию (0 для чисел).

---

### **Слайд 11: Арифметические операции**

```csharp
1.  int a = 10 + 5;   // Сложение: 15
2.  int b = 10 - 5;   // Вычитание: 5
3.  int c = 10 * 5;   // Умножение: 50
4.  int d = 10 / 3;   // Деление целое: 3
5.  int e = 10 % 3;   // Остаток: 1
```

**⚠️ Важно:** Для деления с дробной частью нужен `double`:
```csharp
1.  double result = 10.0 / 3;
2.  // Результат: ~3.333...
```

**📌 Сокращённые операции:** `+=`, `-=`, `*=`, `/=`, `++`, `--`

---


### **Слайд 12: Инкремент и декремент**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**📈 Постфиксная форма (`x++`)** – сначала используется значение:
```csharp
1.  int a = 5;
2.  int b = a++; // b = 5, потом a = 6
```

**📈 Префиксная форма (`++x`)** – сначала увеличивается:
```csharp
1.  int c = 5;
2.  int d = ++c; // c = 6, потом d = 6
```

</div>
<div>

**📊 Полный пример:**
```csharp
1.  int a = 5, b = a++, c = 5, d = ++c;
2.  Console.WriteLine($"a={a}, b={b}, c={c}, d={d}");
3.  // Вывод: a=6, b=5, c=6, d=6
```

</div>
</div>

---

### **Слайд 13: Ввод и вывод данных**

```csharp
1.  Console.Write("Введите имя: ");
2.  string name = Console.ReadLine();
3.  Console.WriteLine($"Привет, {name}!");
```

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 10px; margin-top: 15px;">
<div>

| Метод | Описание |
|-------|----------|
| `Console.Write()` | Вывод без перевода строки |
| `Console.WriteLine()` | Вывод с переводом строки |
| `Console.ReadLine()` | Чтение строки с клавиатуры |

</div>
<div>

**📝 Интерполяция строк:**
```csharp
1.  string product = "яблоки";
2.  int quantity = 5;
3.  Console.WriteLine($"Купите {quantity} кг {product}.");
```

</div>
</div>

---

### **Слайд 14: Типовые ошибки ввода/вывода**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**❌ Ошибки и решения:**

| Ошибка | Пример | Правильно |
|--------|--------|-----------|
| Не преобразовали строку | `int age = input;` | `int.Parse(input)` |
| Сравнение строк | `if (answer == "да")` | `answer.Equals("да", ...)` |
| Конкатенация в цикле | `result += i.ToString();` | `StringBuilder` |

</div>
<div>

**✅ Решение для конкатенации:**
```csharp
1.  StringBuilder result = new StringBuilder();
2.  for (int i = 0; i < 10000; i++)
3.      result.Append(i);
4.  string final = result.ToString();
```

</div>
</div>

---

### **Слайд 15: Условный оператор `if-else`**

```csharp
1.  int number = 10;  
2.  if (number > 0)
3.  {
4.      Console.WriteLine("Число положительное.");
5.      Console.WriteLine("Это сообщение тоже в блоке if.");
6.  }
7.  // else НЕ обязателен!
```

**📌 Фигурные скобки `{ }`:**
- Обозначают **блок кода**
- Если после `if` только **одна** команда – можно опустить
- **💡 Рекомендация:** Всегда использовать для ясности

**Короткая форма:**
```csharp
1.  if (number > 0)
2.      Console.WriteLine("Положительное");
```

---
### **Слайд 16: Типовые ошибки в `if`**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**❌ Ошибка 1:** Точка с запятой
```csharp
1.  if (number > 0); // ; завершает оператор!
2.  {
3.      // Этот блок выполнится ВСЕГДА
4.  }
```

</div>
<div>

**📋 Другие ошибки:**

| Ошибка | Пример | Правильно |
|--------|--------|-----------|
| Присваивание вместо сравнения | `if (x = 5)` | `if (x == 5)` |
| Неправильная группировка | `if (a > b && b > c \|\| c == 0)` | `if ((a > b && b > c) \|\| c == 0)` |
| Проверка диапазона | `if (0 < x < 10)` | `if (x > 0 && x < 10)` |

</div>
</div>

---
### **Слайд 17: Метод `TryParse`**

**⚠️ Проблема:** `int.Parse()` вызывает ошибку при некорректных данных.

**✅ Решение:** `int.TryParse()` – безопасный метод:
```csharp
1.  string input = "123";
2.  int result;
3.  bool success = int.TryParse(input, out result);
4.  // success = true, result = 123
5.  
6.  input = "abc";
7.  success = int.TryParse(input, out result);
8.  // success = false, result = 0
```

**📌 Синтаксис:**
`bool успех = int.TryParse(строка, out int переменная);`

- Возвращает `true`, если удалось преобразовать
- Результат записывается в переменную через `out`

---
### **Слайд 18: Пример `TryParse` с `if`**

```csharp
1.  Console.Write("Введите число: ");
2.  string input = Console.ReadLine();  
3.  if (int.TryParse(input, out int number))
4.  {
5.      if (number > 0)
6.          Console.WriteLine("Число положительное.");
7.      else if (number < 0)
8.          Console.WriteLine("Число отрицательное.");
9.     else
10.         Console.WriteLine("Число равно нулю.");
11. }
12. else
13. {
14.     Console.WriteLine("Это не число!");
15. }
```

**💡 Особенности подхода:**
- `int.TryParse(input, out int number)` – объявление переменной в вызове
- Переменная `number` доступна внутри блока `if`

---
### **Слайд 19: Операторы сравнения**

| Оператор | Описание | Пример | Результат |
|----------|----------|--------|-----------|
| `==` | Равно | `5 == 5` | `true` |
| `!=` | Не равно | `5 != 3` | `true` |
| `>` | Больше | `5 > 3` | `true` |
| `<` | Меньше | `5 < 3` | `false` |
| `>=` | Больше или равно | `5 >= 5` | `true` |
| `<=` | Меньше или равно | `5 <= 3` | `false` |

**📌 Все операторы сравнения возвращают `bool` (true/false).**

---
### **Слайд 20: Логические операторы**

| Оператор | Название | Описание | Пример | Результат |
|----------|----------|----------|--------|-----------|
| `&&` | И (AND) | Оба true | `(5 > 3) && (2 < 4)` | `true` |
| `\|\|` | ИЛИ (OR) | Хотя бы одно true | `(5 < 3) \|\| (2 < 4)` | `true` |
| `!` | НЕ (NOT) | Инверсия значения | `!(5 < 3)` | `true` |

**📊 Приоритет операторов:**
1. `!` (НЕ)
2. `>` `<` `>=` `<=` `==` `!=` (сравнения)
3. `&&` (И)
4. `||` (ИЛИ)

**💡 Лучше использовать скобки для явной группировки.**

---
### **Слайд 21: Оператор `switch` (классический)**

```csharp
1.  int day = 3;
2.  string dayName;  
3.  switch (day)
4.  {
5.      case 1:
6.          dayName = "Понедельник";
7.          break;
8.      case 2:
9.         dayName = "Вторник";
10.         break;
11.     default:
12.         dayName = "Неизвестный день";
13.  }
```

**📌 Важные правила:**
1. Каждый `case` заканчивается `break`, 
2. `default` выполняется, если ни один `case` не подошёл
3. Работает с `int`, `char`, `string` и др.

---
### **Слайд 22: Новый синтаксис `switch` (C# 8.0+)**

```csharp
1.  int score = 85;
2.  string grade = score switch
3.  {
4.      >= 90 => "Отлично",
5.      >= 75 => "Хорошо",
6.      >= 60 => "Удовлетворительно",
7.      _ => "Неудовлетворительно"
8.  };
9.  // _ - аналог default
10. Console.WriteLine($"Оценка: {grade}");
```

```csharp
1.  string result = input switch
2.  {
3.      "да" or "yes" => "Согласие",
4.      "нет" or "no" => "Отказ",
5.      _ => "Неизвестный ответ"
6.  };
```

**💡 Особенности:** `=>` вместо `:`, возвращает значение, не требует `break`

---
### **Слайд 23: Цикл `for`**

```csharp
1.  // Вывод чисел от 1 до 5
2.  for (int i = 1; i <= 5; i++)
3.  {
4.      Console.WriteLine($"Шаг {i}");
5.  }
```

**📌 Синтаксис:**
`for (инициализация; условие; итератор) { тело }`

**🔍 Разбор:**
- `int i = 1` – инициализация (выполняется 1 раз)
- `i <= 5` – условие продолжения цикла
- `i++` – увеличение счётчика после каждой итерации

**🎯 Используется,** когда известно точное количество повторений.

---
### **Слайд 24: Циклы `while` и `do-while`**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**🔄 Цикл `while`** – проверяет условие **перед**:
```csharp
1.  int x = 0;
2.  while (x < 3)
3.  {
4.      Console.WriteLine(x);
5.      x++; // Не забудьте изменять условие!
6.  }
```

</div>
<div>

**🔄 Цикл `do-while`** – проверяет условие **после**:
```csharp
1.  string answer;
2.  do {
3.      Console.Write("Повторить? (да/нет): ");
4.      answer = Console.ReadLine();
5.  } while (answer == "да");
```


- `while` проверяет условие перед итерацией => гарантия выполнения 0+ раз
 - `do-while` проверяет условие после итерации => гарантия выполнения 1+ раз

---
### **Слайд 25: Цикл `foreach`**

```csharp
1.  string[] colors = {"красный", "зеленый", "синий"};
2.  foreach (string color in colors)
3.  {
4.      Console.WriteLine(color);
5.  }
6.  // Выведет: красный, зеленый, синий
```

**⏹️ Оператор `break`** – немедленно завершает цикл:
```csharp
1.  for (int i = 0; i < 10; i++)
2.  {
3.      if (i == 5)
4.      {
5.          break; // Выход при i = 5
6.      }
7.      Console.WriteLine(i);
8.      // Выведет: 0,1,2,3,4
9.  }
```

**💡 `foreach` используется для перебора коллекций.**

---
### **Слайд 26: Оператор `continue`**

```csharp
1.  for (int i = 0; i < 5; i++)
2.  {
3.      if (i == 2)
4.      {
5.          continue; // Пропускаем i = 2
6.      }
7.      Console.WriteLine(i);
8.      // Выведет: 0,1,3,4
9.  }
```

**⏭️ Оператор `continue`** – прерывает текущую итерацию и переходит к следующей.

| Оператор | Действие | Эмодзи |
|----------|----------|--------|
| `break` | Выход из всего цикла | ⏹️ |
| `continue` | Пропуск текущей итерации | ⏭️ |

---
### **Слайд 27: Типовые ошибки в циклах (1)**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**❌ Ошибка 1:** Бесконечный цикл
```csharp
1.  int x = 0;
2.  while (x < 5) // x не меняется
3.  {
4.      Console.WriteLine(x);
5.      // Забыли x++ - бесконечный цикл!
6.  }
```
</div>
<div>

**📋 Другие ошибки:**

| Ошибка | Пример | Правильно |
|--------|--------|-----------|
| Неправильное условие | `for (int i=0; i=5; i++)` | `for (int i=0; i<5; i++)` |
| Дублирование имён | Вложенные циклы с `i` | Разные имена |

</div>
</div>

---
### **Слайд 28: Типовые ошибки в циклах (2)**

<div style="display: grid; grid-template-columns: 1fr 1fr; gap: 20px;">
<div>

**❌ Ошибка:** Изменение коллекции в `foreach`
```csharp
1.  List<int> numbers = new List<int> {1, 2, 3};
2.  foreach (int num in numbers)
3.  {
4.      if (num == 2)
5.          numbers.Remove(num); // ОШИБКА!
6.  }
```

</div>
<div>

**✅ Решение:** Использовать `for`
```csharp
1.  for (int i = 0; i < numbers.Count; i++)
2.  {
3.      if (numbers[i] == 2) {
4.          numbers.RemoveAt(i);
5.          i--; // Корректируем индекс
6.      }
7.  }
```

</div>
</div>

**⚠️ Важно:** Нельзя изменять коллекцию во время перебора в `foreach`.

---
### **Слайд 29: Практический пример**

```csharp
1.  int sum = 0;
2.  Console.WriteLine("Вводите числа. Для завершения введите 0.");
3.  
4.  while (true)
5.  {
6.      Console.Write("Введите число: ");
7.      string input = Console.ReadLine();
8.      
9.      if (!int.TryParse(input, out int number))
10.     {
11.         Console.WriteLine("Ошибка! Введите целое число.");
12.         continue; // Переходим к следующей итерации
13.     }
14.     
15.     if (number == 0) break; // Выход из цикла
16.         
17.     if (number > 0) sum += number; // Добавляем положительное
18. }
19. 
20. Console.WriteLine($"Сумма положительных чисел: {sum}");
```
---
### **Слайд 29-1: Практический пример**

**🎯 Что делает программа:**
1. Запрашивает числа у пользователя
2. Использует `TryParse` для безопасного ввода
3. `continue` при ошибке ввода
4. `break` при вводе 0
5. Суммирует положительные числа

---
### **Слайд 30: Разбор кода №1 (вопрос)**

```csharp
1.  int x = 5;
2.  int y = 3;
3.  
4.  if (x > y && y > 0)
5.  {
6.      Console.WriteLine("Условие 1 выполнено");
7.  }
8.  
9.  if (x < y || y > 0)
10. {
11.     Console.WriteLine("Условие 2 выполнено");
12. }
13. 
14. if (!(x == y))
15. {
16.     Console.WriteLine("Условие 3 выполнено");
17. }
```

**❓ Вопрос:** Что выведет эта программа?

---
### **Слайд 31: Ответ к коду №1**
**🔍 Пошаговое выполнение:**
1. `x = 5`, `y = 3`
2. `x > y` → `true`, `y > 0` → `true`  
   `true && true` → `true`  
   **Вывод:** `"Условие 1 выполнено"`
3. `x < y` → `false`, `y > 0` → `true`  
   `false || true` → `true`  
   **Вывод:** `"Условие 2 выполнено"`
4. `x == y` → `false`, `!(false)` → `true`  
   **Вывод:** `"Условие 3 выполнено"`

**📤 Итоговый вывод:**
```
Условие 1 выполнено
Условие 2 выполнено
Условие 3 выполнено
```

---
### **Слайд 32: Разбор кода №2 (вопрос)**

```csharp
1.  int sum = 0;
2.  for (int i = 1; i <= 5; i++)
3.  {
4.      if (i % 2 == 0)
5.      {
6.          continue;
7.      }
8.      sum += i;
9.      Console.WriteLine($"i={i}, sum={sum}");
10. }
11. Console.WriteLine($"Итог: {sum}");
```

**❓ Вопрос:** Что выведет эта программа и почему?

---
### **Слайд 33: Ответ к коду №2**

**🔍 Пошаговое выполнение:**
1. `i = 1`: `1 % 2 = 1` → пропускаем `continue`  
   `sum = 0 + 1 = 1`  
   **Вывод:** `"i=1, sum=1"`
2. `i = 2`: `2 % 2 = 0` → `continue`, пропускаем остаток
3. `i = 3`: `3 % 2 = 1` → `sum = 1 + 3 = 4`  
   **Вывод:** `"i=3, sum=4"`
4. `i = 4`: `4 % 2 = 0` → `continue`
5. `i = 5`: `5 % 2 = 1` → `sum = 4 + 5 = 9`  
   **Вывод:** `"i=5, sum=9"`

**🎯 Что делает программа:** Суммирует нечётные числа от 1 до 5.

---
### **Слайд 34: Задачи на линейные алгоритмы**

<div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 15px;">
<div>

**📝 Задача 1.1:**  
Запросить два целых числа и вывести их сумму, разность и произведение.

</div>
<div>

**🌡️ Задача 1.2:**  
Перевод температуры из °C в °F.  
Формула: `F = C × 9/5 + 32`

</div>
<div>

**📐 Задача 1.3:**  
Вычисление площади и периметра прямоугольника.

</div>
</div>

---
### **Слайд 35: Задачи на условные операторы**

<div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 15px;">
<div>

**🔢 Задача 2.1:**  
Определить, является ли число чётным или нечётным.

</div>
<div>

**📊 Задача 2.2:**  
Запросить три числа и вывести наибольшее.

</div>
<div>

**🧮 Задача 2.3:**  
Программа-калькулятор: два числа и операция (+, -, *, /).  
*Учтите деление на ноль!*

</div>
</div>

---
### **Слайд 36: Задачи на циклы**

<div style="display: grid; grid-template-columns: 1fr 1fr 1fr; gap: 15px;">
<div>

**🔄 Задача 3.1:**  
Вывести числа от 1 до N, кратные 3.

</div>
<div>

**📈 Задача 3.2:**  
Вычислить факториал числа N (N! = 1×2×...×N).

</div>
<div>

**🔐 Задача 3.3:**  
Запрашивать пароль до ввода `"12345"`.

</div>
</div>

---
### **Слайд 37: Что будет на следующей лекции?**

**📚 Лекция 2: C#: Методы, массивы, основы ООП**

**🎯 Цель:** Организовывать код, работать с коллекциями данных, понимать разницу в хранении данных и эффективно работать с текстом.

**Следующая лекция:** Глубокое погружение в организацию кода и работу с данными в C#!

---
### **Слайд 38: Ключевые моменты лекции 1**

1. **Структура программы:** `Main` – точка входа, `{ }` – блоки кода
2. **Типы данных:** `int`, `double`, `bool`, `string`, `char`
3. **Приведение типов:** явное/неявное и класс `Convert`
4. **Управление потоком:**  
   - `if-else` (всегда со скобками!)
   - `switch` (классический и новый синтаксис)
   - `for`, `while`, `do-while`, `foreach`
   - `break` и `continue`

---
### **Слайд 39: Ключевые моменты (продолжение)**

5. **Типовые ошибки новичков:**  
   - Точка с запятой после `if`
   - Присваивание (`=`) вместо сравнения (`==`)
   - Бесконечные циклы (забыли изменить условие)
   - Изменение коллекций в `foreach`

6. **Безопасный ввод:** `int.TryParse()` вместо `int.Parse()`
7. **Вывод с интерполяцией:** `$"Текст {переменная}"`

**📚 Дальнейшие шаги:**  
Практика написания небольших программ с использованием всех изученных конструкций.